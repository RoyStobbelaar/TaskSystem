<div ng-controller="app.views.home as vm">
    <h1>{{vm.title}}</h1>
    <div class="row">
        <div class="col-md-12">
            {{vm.welcomeMessage}}
           {{welcomeMessage}}
        </div>

    </div>
</div>